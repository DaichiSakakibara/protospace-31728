<div class="main">
  <div class="inner">
    <div class="form__wrapper">
      <h2 class="page-heading">新規プロトタイプ投稿</h2>
        <%# 部分テンプレートでフォームを表示する %>
  <%= form_with model: @prototype, url: prototypes_path, local: true do |f|%>
            <%= render partial: "form", locals: { f: f}%>
  <% end %>
    </div>
  </div>
</div>